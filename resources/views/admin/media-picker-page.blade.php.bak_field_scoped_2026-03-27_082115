<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>اختيار الوسائط</title>
<style>
body{font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:#f3f4f6;margin:0;padding:24px;color:#111827}
.wrap{max-width:1200px;margin:0 auto}
.topbar{display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap;margin-bottom:20px}
h1{margin:0;font-size:28px}
.tabs{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:18px}
.tab-btn{display:inline-block;text-decoration:none;padding:10px 14px;border-radius:12px;font-weight:700;border:1px solid #d1d5db;background:#fff;color:#111827}
.tab-btn.active{background:#127962;color:#fff;border-color:#127962}
.panel{background:#fff;border:1px solid #e5e7eb;border-radius:18px;padding:18px;box-shadow:0 4px 16px rgba(0,0,0,.04)}
.toolbar{display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin-bottom:18px}
.search{width:min(360px,100%);border:1px solid #d1d5db;border-radius:12px;padding:12px 14px;background:#fff}
.btn{background:#111827;color:#fff;padding:10px 14px;border:none;border-radius:12px;cursor:pointer;font-weight:700;text-decoration:none;display:inline-block}
.btn-green{background:#127962}
.btn-light{background:#eef2f7;color:#111827}
.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(190px,1fr));gap:16px}
.card{background:#fff;border-radius:16px;padding:10px;border:1px solid #e5e7eb;box-shadow:0 1px 3px rgba(0,0,0,.04)}
.card img{width:100%;height:130px;object-fit:cover;border-radius:12px;border:1px solid #eee;margin-bottom:8px}
.file-box{height:130px;border-radius:12px;border:1px solid #eee;margin-bottom:8px;display:flex;align-items:center;justify-content:center;background:#f8fafc;font-weight:800;color:#dc2626;font-size:24px}
.meta-id{font-size:12px;font-weight:700;color:#111827}
.meta-title{font-size:12px;color:#6b7280;line-height:1.5;word-break:break-word;margin-top:4px;min-height:34px}
.choose-btn{width:100%;margin-top:10px;background:#111827;color:#fff;padding:10px 12px;border:none;border-radius:12px;cursor:pointer;font-weight:700;text-decoration:none;display:block;text-align:center}
.empty{padding:36px;border:1px dashed #d1d5db;border-radius:16px;text-align:center;color:#6b7280;background:#fff}
.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}
.form-grid .full{grid-column:1/-1}
.input{width:100%;border:1px solid #d1d5db;border-radius:12px;padding:12px 14px;background:#fff;box-sizing:border-box}
.label{display:block;margin-bottom:6px;font-weight:700}
.notice{margin-bottom:16px;padding:12px 14px;border-radius:12px;background:#ecfdf5;color:#166534;font-weight:700}
@media (max-width: 768px){body{padding:16px}.form-grid{grid-template-columns:1fr}}
</style>
</head>
<body>
<div class="wrap">
    <div class="topbar">
        <h1>اختيار الوسائط</h1>
        @if($returnUrl)
            <a href="{{ $returnUrl }}" class="btn btn-light">العودة</a>
        @endif
    </div>

    <div class="tabs">
        <a href="{{ url('/admin/media-picker') . '?' . http_build_query(['field' => $field, 'return' => $returnUrl, 'tab' => 'library']) }}"
           class="tab-btn {{ $activeTab === 'library' ? 'active' : '' }}">
            مكتبة الوسائط
        </a>

        <a href="{{ url('/admin/media-picker') . '?' . http_build_query(['field' => $field, 'return' => $returnUrl, 'tab' => 'upload']) }}"
           class="tab-btn {{ $activeTab === 'upload' ? 'active' : '' }}">
            رفع جديد
        </a>
    </div>

    <div class="panel">
        @if(request()->boolean('uploaded'))
            <div class="notice">تم رفع الملف بنجاح. يمكنك الآن اختياره من المكتبة.</div>
        @endif

        @if($activeTab === 'upload')
            <form method="POST" action="{{ url('/admin/media-picker/upload') }}" enctype="multipart/form-data">
                @csrf
                <input type="hidden" name="field" value="{{ $field }}">
                <input type="hidden" name="return" value="{{ $returnUrl }}">

                <div class="form-grid">
                    <div>
                        <label class="label">عنوان الملف</label>
                        <input class="input" type="text" name="title" value="{{ old('title') }}">
                    </div>

                    <div>
                        <label class="label">النص البديل</label>
                        <input class="input" type="text" name="alt_text" value="{{ old('alt_text') }}">
                    </div>

                    <div class="full">
                        <label class="label">الملف</label>
                        <input class="input" type="file" name="file" accept=".jpg,.jpeg,.png,.webp,.gif,.pdf,.svg,.doc,.docx,.xls,.xlsx" required>
                    </div>

                    <div class="full" style="display:flex;justify-content:flex-end;gap:10px;flex-wrap:wrap;">
                        <a href="{{ url('/admin/media-picker') . '?' . http_build_query(['field' => $field, 'return' => $returnUrl, 'tab' => 'library']) }}"
                           class="btn btn-light">
                            رجوع للمكتبة
                        </a>

                        <button type="submit" class="btn btn-green">رفع الملف</button>
                    </div>
                </div>
            </form>
        @else
            <form method="GET" action="{{ url('/admin/media-picker') }}">
                <input type="hidden" name="field" value="{{ $field }}">
                <input type="hidden" name="return" value="{{ $returnUrl }}">
                <input type="hidden" name="tab" value="library">

                <div class="toolbar">
                    <input class="search" type="text" name="q" value="{{ $search }}" placeholder="ابحث بالعنوان أو الرقم أو الامتداد...">
                    <button type="submit" class="btn btn-light">بحث</button>
                </div>
            </form>

            @if($items->isEmpty())
                <div class="empty">لا توجد ملفات في مكتبة الوسائط لهذه الجمعية.</div>
            @else
                <div class="grid">
                    @foreach($items as $item)
                        <div class="card">
                            @if($item->is_image)
                                <img src="{{ asset('storage/' . $item->file) }}" alt="{{ $item->alt_text ?: $item->title }}">
                            @else
                                <div class="file-box">{{ strtoupper($item->extension ?: 'FILE') }}</div>
                            @endif

                            <div class="meta-id">#{{ $item->id }}</div>
                            <div class="meta-title">{{ $item->title ?: basename((string) $item->file) }}</div>

                            <a
                                href="{{ $returnUrl ? $returnUrl . (str_contains($returnUrl, '?') ? '&' : '?') . 'selected_media_id=' . $item->id . '&selected_media_file=' . urlencode($item->file) : '#' }}"
                                class="choose-btn"
                            >
                                استخدام هذا الملف
                            </a>
                        </div>
                    @endforeach
                </div>
            @endif
        @endif
    </div>
</div>
</body>
</html>
